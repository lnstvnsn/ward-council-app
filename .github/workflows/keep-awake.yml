name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 0 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: wget -q -O /dev/null -U "Mozilla/5.0" https://ward-council-assignments.streamlit.app/
